import * as React from 'react';
import { View } from 'react-native';
import { SearchBar as SearchBarInternal } from 'react-native-elements';

import styles from './styles';

const SearchBar = () => (
  <SearchBarInternal />
);
export default SearchBar;
